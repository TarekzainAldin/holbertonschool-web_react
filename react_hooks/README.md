# Dashboard - Header Component

This task converts the `Header` component from a class-based component to a functional one using React Hooks (`useContext`). It also ensures all functionalities like logout and user display are preserved.
